# GroupLogic-IDE
GroupLogic IDE — an advanced group generator that's not really random. Code your grouping rules with a custom DSL: preferences, scores, roles, weights, and classroom layouts. Monte Carlo + Hill Climbing optimization under the hood.
